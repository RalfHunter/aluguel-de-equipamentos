# Plano de Teste para Model Equipamento (Sprint V)

| Funcionalidade                      | Comportamento Esperado                                                                                                                               | Verificações                                                                                                       | Critérios de Aceite                                                                                                    |
|-----------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------|
| Cadastro válido de equipamento     | Um equipamento com todos os campos obrigatórios e válidos deve ser salvo com sucesso                                                                | Inserir um equipamento com todos os campos obrigatórios preenchidos corretamente                                   | O equipamento é salvo e retornado com `_id`, `equiStatus` como `"pendente"`, `equiNotaMediaAvaliacao` como `0`, e `equiAvaliacoes` como `[]` |
| Limite máximo de fotos             | O equipamento deve permitir no máximo 5 fotos                                                                                                       | Tentar salvar equipamento com 5 fotos                                                                              | O equipamento é salvo com exatamente 5 fotos                                                                           |
| Campos obrigatórios                | Não deve ser possível cadastrar um equipamento sem nome, descrição, valor da diária, categoria, fotos completas ou quantidade disponível           | Tentar salvar equipamento omitindo cada um desses campos                                                          | A operação deve falhar com erro de validação (`mongoose.Error.ValidationError`)                                          |
| Fotos com campos obrigatórios      | Cada foto deve ter os campos `url`, `largura`, `altura` e `tamanhoMb`                                                                                | Tentar salvar equipamento com fotos faltando algum desses campos                                                  | A operação deve falhar com erro de validação                                                                            |
| Campo equiStatus com valor padrão  | Ao cadastrar um equipamento, o campo `equiStatus` deve iniciar como `"pendente"`                                                                    | Salvar equipamento sem informar `equiStatus`                                                                       | O documento salvo deve ter `equiStatus` igual a `"pendente"`                                                           |
| Campo equiNotaMediaAvaliacao padrão | Ao cadastrar um equipamento, a nota média de avaliação deve ser 0                                                                                   | Salvar equipamento e verificar valor de `equiNotaMediaAvaliacao`                                                  | O documento salvo deve ter `equiNotaMediaAvaliacao` igual a `0`                                                        |
| Campo equiAvaliacoes padrão         | O array de avaliações deve iniciar vazio                                                                                                            | Salvar equipamento e verificar valor de `equiAvaliacoes`                                                          | O documento salvo deve ter `equiAvaliacoes` como array vazio                                                            |
| Leitura de equipamentos cadastrados | O sistema deve retornar todos os equipamentos cadastrados                                                                                           | Inserir múltiplos equipamentos e fazer consulta com `find()`                                                      | O resultado deve conter todos os equipamentos cadastrados com seus respectivos nomes                                     |

# Plano de Teste para Controller (Sprint V)

| Funcionalidade               | Comportamento Esperado                                                      | Verificações                                                              | Critérios de Aceite                                                                                       |
|-----------------------------|----------------------------------------------------------------------------|---------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| Listagem de equipamentos     | Deve listar equipamentos com query válida ou todos se não houver filtro    | Enviar requisição com ou sem parâmetros de query                          | Resposta contém array com equipamentos filtrados ou todos os equipamentos cadastrados                     |
| Validação de query           | Deve validar query e retornar erro para query inválida                     | Enviar query inválida                                                      | Retorna erro 400 (ZodError)                                                                                |
| Permissão para listar pendentes | Usuários comuns não podem listar equipamentos com status "pendente"         | Enviar query com status "pendente" sem permissão admin                    | Retorna erro 403                                                                                           |
| Listar pendentes para admin  | Usuário admin pode listar equipamentos pendentes                           | Enviar query com status "pendente" como admin                            | Retorna lista de equipamentos pendentes                                                                   |
| Busca por ID                | Retorna equipamento pelo ID válido                                        | Enviar ID válido                                                         | Retorna dados do equipamento correspondente                                                               |
| Validação de ID no get       | Retorna erro para ID inválido                                              | Enviar ID inválido                                                       | Retorna erro 400 (ZodError)                                                                                |
| Acesso a equipamento não ativo | Usuário só pode acessar equipamentos ativos ou próprios                   | Enviar ID de equipamento não ativo e não próprio                         | Retorna erro 403                                                                                           |
| Cadastro de equipamento      | Deve criar equipamento com dados válidos e fotos                           | Enviar dados válidos com ou sem fotos                                    | Cria equipamento, retorna mensagem de sucesso e status "pendente"                                         |
| Validação de dados no cadastro | Retorna erro para dados inválidos                                          | Enviar dados inválidos                                                   | Retorna erro 400 (ZodError)                                                                                |
| Atualização de equipamento   | Atualiza equipamento com dados válidos e ID válido                         | Enviar ID e dados válidos                                                | Atualiza equipamento, retorna mensagem de sucesso                                                         |
| Validação de dados na atualização | Retorna erro para ID ou dados inválidos                                  | Enviar ID inválido ou dados inválidos                                   | Retorna erro 400 (ZodError)                                                                                |
| Permissão na atualização     | Apenas dono do equipamento pode atualizar                                 | Enviar atualização por usuário que não é dono                           | Retorna erro 403                                                                                           |
| Aprovação/Reprovação         | Usuário admin pode aprovar ou reprovar equipamentos                        | Enviar ID válido por usuário admin                                      | Equipamento aprovado/reprovado com mensagem de sucesso                                                    |
| Permissão para aprovar/reprovar | Usuários não admin não podem aprovar ou reprovar                         | Enviar requisição por usuário sem permissão admin                       | Retorna erro 403                                                                                           |
| Atualizar status             | Atualiza status do equipamento com dados válidos                          | Enviar ID e status válido                                               | Atualiza status, retorna mensagem de sucesso                                                              |
| Adicionar fotos              | Adiciona fotos ao equipamento com ID válido                               | Enviar fotos e ID válido                                                | Fotos adicionadas com sucesso, retorna mensagem                                                           |
| Listar foto                 | Retorna foto do equipamento conforme IDs válidos                          | Enviar ID do equipamento e ID da foto válidos                           | Retorna foto com header correto                                                                            |
| Validação em operações       | Retorna erro para IDs inválidos, dados inválidos ou ausência de autenticação | Enviar IDs ou dados inválidos, ou ausência de token                      | Retorna erro 400, 401 ou 403 conforme o caso                                                               |
| Erros inesperados            | Retorna erro 500 para falhas internas simuladas                           | Simular erros internos nos serviços                                     | Retorna erro 500 com mensagem de erro interno                                                             |


# Plano de Testes para Service (Sprint V)

| Funcionalidade                  | Comportamento Esperado                                                                                  | Verificações                                                                                     | Critérios de Aceite                                                                                              |
|--------------------------------|--------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------|
| Listagem de Equipamentos        | Deve processar filtros recebidos e retornar lista de equipamentos filtrados corretamente               | Chamar listar() com filtros variados (categoria, status, faixa de valor, usuário); verificar se os filtros são processados corretamente e se o repositório é chamado com query, página e limite; testar com e sem usuário autenticado | Retorna lista filtrada conforme os parâmetros; filtros aplicados corretamente; status padrão "ativo" quando não especificado |
| Listagem de Equipamentos Pendentes | Deve retornar lista de equipamentos pendentes apenas para admin/moderador                           | Chamar listar() com status "pendente" e usuário admin; verificar chamada ao repositório com query correta | Retorna lista de equipamentos pendentes; erro 403 para usuários sem permissão                             |
| Busca por ID                   | Deve retornar o equipamento se existir e se o usuário tiver permissão para visualizá-lo                | Buscar equipamento por ID; testar retorno do equipamento quando ativo; lançar erro 403 se usuário não é dono e equipamento inativo; lançar erro 404 se não encontrado | Equipamento retornado se usuário autorizado; erros 403 e 404 lançados apropriadamente                           |
| Cadastro de Equipamento         | Deve validar campos obrigatórios e fotos, criar equipamento com status pendente e associar usuário     | Criar equipamento com dados completos e corretos; lançar erro se faltar nome, categoria ou fotos | Equipamento criado com dados corretos e status pendente; erros lançados para dados obrigatórios faltantes       |
| Validação de Campos no Cadastro | Deve garantir que campos obrigatórios (nome, categoria) não estejam ausentes e fotos sejam array não vazio | Tentar criar com dados incompletos (sem nome, sem categoria, fotos vazias ou não array)         | Erro 400 lançado para ausência de campos obrigatórios ou fotos inválidas                                       |
| Atualização de Equipamento      | Deve permitir atualizar apenas campos permitidos (valor, quantidade disponível) e bloquear se pendente | Atualizar equipamento ativo com campos permitidos; tentar atualizar equipamento pendente; bloquear atualizações indevidas ou campos não permitidos | Atualização realizada com sucesso quando válida; erro 403 para equipamentos pendentes; erro 400 para campos proibidos |
| Atualização de Equipamento Inexistente | Deve lançar erro 404 se o equipamento não existir                                            | Tentar atualizar com ID inexistente                                                          | Erro 404 lançado com mensagem de equipamento não encontrado                                                    |
| Atualização de Status           | Deve permitir dono do equipamento mudar status (ativo/inativo); bloquear se pendente ou com reservas ativas | Atualizar status para ativo/inativo; testar com equipamento pendente, com reservas ativas, usuário não dono, ou status já aplicado | Status atualizado com sucesso; erro 403 para não donos ou pendentes; erro 409 para reservas ativas; erro 400 para transições inválidas |
| Aprovação de Equipamento        | Deve permitir admin/moderador aprovar equipamento pendente, mudando status para ativo                 | Aprovar equipamento pendente com usuário admin; testar com usuário sem permissão ou equipamento não pendente | Equipamento aprovado e status alterado para ativo; erro 403 para usuários sem permissão ou equipamentos não pendentes |
| Reprovação de Equipamento       | Deve permitir admin/moderador excluir equipamento pendente                                            | Reprovar equipamento pendente com usuário admin; testar com usuário sem permissão ou equipamento não pendente | Equipamento excluído com sucesso; erro 403 para usuários sem permissão ou equipamentos não pendentes             |
| Adição de Fotos                 | Deve permitir adicionar fotos a equipamento existente                                                 | Adicionar fotos válidas a equipamento; testar com equipamento inexistente ou fotos inválidas   | Fotos adicionadas com sucesso; erro 404 para equipamento inexistente; erro 400 para fotos inválidas             |
| Listagem de Foto                | Deve retornar informações da foto se equipamento e foto existirem e arquivo estiver no servidor        | Buscar foto por ID; testar com equipamento inexistente, foto inexistente ou arquivo ausente     | Informações da foto retornadas com sucesso; erro 404 para equipamento, foto ou arquivo não encontrado           |                                                                         |

# Plano de Testes para Repository (Sprint V)

| Funcionalidade | Comportamento Esperado | Verificações | Critérios de Aceite |
|----------------|-----------------------|--------------|---------------------|
| Inicialização do repositório | Usa `EquipamentoModel` como padrão se nenhum modelo for fornecido | Instanciar `EquipamentoRepository` sem modelo e verificar o atributo `model` | Repositório usa `EquipamentoModel` como modelo padrão |
| Cadastro de equipamento | Cria e salva equipamento com status inativo | Passar dados válidos (`nome`, `descricao`, `categoria`, `valorDiaria`, `disponibilidade`) e verificar se `save` é chamado | Equipamento é salvo com `ativo=false` e retorna com `_id` |
| Cadastro com atributos obrigatórios ausentes | Lança erro se faltarem campos obrigatórios | Passar dados incompletos e verificar se `save` rejeita | Lança erro "Missing required fields"; equipamento não é salvo |
| Listagem de equipamentos | Lista equipamentos com filtros de categoria, preço e disponibilidade | Passar query com `categoria`, `valorDiariaMin`, `valorDiariaMax`, `disponibilidade`, `page` e `limit`, e verificar chamada de `paginate` | Retorna equipamentos filtrados; `paginate` usa query, `page`, `limit` e `sort: { equiNome: 1 }` |
| Listagem de equipamentos ativos | Lista apenas equipamentos com `ativo=true` | Passar query com `ativo=true`, `page` e `limit`, e verificar chamada de `paginate` | Retorna equipamentos ativos; `paginate` inclui `ativo=true` |
| Busca por ID | Retorna equipamento ativo pelo ID | Passar ID válido e verificar se `findById` retorna equipamento | Retorna equipamento; `findById` é chamado com ID |
| Busca por ID inexistente | Retorna null se equipamento não existe | Passar ID inexistente e verificar se `findById` retorna null | Retorna null; `findById` é chamado com ID |
| Atualização de equipamento | Atualiza equipamento e marca como inativo | Passar ID válido e dados (`nome`, `valorDiaria`) e verificar se `findByIdAndUpdate` atualiza | Equipamento é atualizado com `ativo=false`; retorna equipamento atualizado; usa `{ new: true }` |
| Atualização de equipamento inexistente | Retorna null se equipamento não existe | Passar ID inexistente e dados, e verificar se `findByIdAndUpdate` retorna null | Retorna null; `findByIdAndUpdate` é chamado com ID |
| Remoção de equipamento | Inativa equipamento sem locações ativas | Passar ID válido e verificar se `findByIdAndDelete` é chamado | Equipamento é removido; retorna equipamento removido; `findByIdAndDelete` é chamado |
| Remoção de equipamento inexistente | Retorna null se equipamento não existe | Passar ID inexistente e verificar se `findByIdAndDelete` retorna null | Retorna null; `findByIdAndDelete` é chamado com ID |

# Plano de Testes para FilterBuilder (Sprint V)

| Funcionalidade | Comportamento Esperado | Verificações | Critérios de Aceite |
|----------------|-----------------------|--------------|---------------------|
| Inicialização do builder | Inicia com filtros vazios | Criar instância de `EquipamentoFilterBuilder` e verificar o estado inicial do atributo `filtros` | O atributo `filtros` é um objeto vazio (`{}`) |
| Filtro por categoria | Adiciona filtro de categoria quando valor válido é fornecido | Passar uma categoria válida ao método `comCategoria` e verificar se `equiCategoria` é adicionado aos filtros | Filtro `equiCategoria` é definido com o valor fornecido; método retorna o próprio builder |
| Filtro por categoria inválida | Não adiciona filtro se categoria é undefined, null ou string vazia | Passar `undefined`, `null` ou `""` ao método `comCategoria` e verificar se `equiCategoria` permanece ausente | Filtro `equiCategoria` não é definido; método retorna o próprio builder |
| Filtro por status | Adiciona filtro de status quando valor booleano é fornecido | Passar `true` ou `false` ao método `comStatus` e verificar se `equiStatus` é adicionado aos filtros | Filtro `equiStatus` é definido com o valor booleano; método retorna o próprio builder |
| Filtro por status inválido | Não adiciona filtro se status não é booleano, é undefined ou null | Passar `"true"`, `undefined` ou `null` ao método `comStatus` e verificar se `equiStatus` permanece ausente | Filtro `equiStatus` não é definido; método retorna o próprio builder |
| Filtro por faixa de valor (apenas mínimo) | Adiciona filtro com valor mínimo quando apenas `minValor` é fornecido | Passar um valor numérico para `minValor` e `undefined` para `maxValor` no método `comFaixaDeValor` e verificar o filtro `equiValorDiaria` | Filtro `equiValorDiaria` é `{ $gte: minValor }`; método retorna o próprio builder |
| Filtro por faixa de valor (apenas máximo) | Adiciona filtro com valor máximo quando apenas `maxValor` é fornecido | Passar `undefined` para `minValor` e um valor numérico para `maxValor` no método `comFaixaDeValor` e verificar o filtro `equiValorDiaria` | Filtro `equiValorDiaria` é `{ $lte: maxValor }`; método retorna o próprio builder |
| Filtro por faixa de valor (mínimo e máximo) | Adiciona filtro com faixa de valor quando ambos `minValor` e `maxValor` são fornecidos | Passar valores numéricos para `minValor` e `maxValor` no método `comFaixaDeValor` e verificar o filtro `equiValorDiaria` | Filtro `equiValorDiaria` é `{ $gte: minValor, $lte: maxValor }`; método retorna o próprio builder |
| Filtro por faixa de valor inválido | Não adiciona filtro se ambos `minValor` e `maxValor` são undefined | Passar `undefined` para ambos `minValor` e `maxValor` no método `comFaixaDeValor` e verificar se `equiValorDiaria` permanece ausente | Filtro `equiValorDiaria` não é definido; método retorna o próprio builder |
| Conversão de strings numéricas em faixa de valor | Converte strings numéricas para números em `minValor` e `maxValor` | Passar strings numéricas ou com espaços ao método `comFaixaDeValor` e verificar o filtro `equiValorDiaria` | Filtro `equiValorDiaria` usa valores numéricos convertidos; método retorna o próprio builder |
| Construção de filtros | Retorna objeto de filtros vazio se nenhum filtro foi adicionado | Chamar `build` sem adicionar filtros e verificar o resultado | Retorna `{}` |
| Construção de filtro de categoria | Retorna objeto com filtro de categoria quando adicionado | Adicionar filtro com `comCategoria` e chamar `build` para verificar o resultado | Retorna `{ equiCategoria: valor }` |
| Construção de filtro de status | Retorna objeto com filtro de status quando adicionado | Adicionar filtro com `comStatus` e chamar `build` para verificar o resultado | Retorna `{ equiStatus: valor }` |
| Construção de filtro de faixa de valor | Retorna objeto com filtro de faixa de valor quando adicionado | Adicionar filtro com `comFaixaDeValor` e chamar `build` para verificar o resultado | Retorna `{ equiValorDiaria: { $gte: minValor, $lte: maxValor } }` |
| Construção de múltiplos filtros | Retorna objeto com todos os filtros adicionados | Adicionar filtros com `comCategoria`, `comStatus` e `comFaixaDeValor` e chamar `build` para verificar o resultado | Retorna objeto com todos os filtros |
| Encadeamento de métodos | Permite encadear métodos e construir filtros corretamente | Encadear chamadas a `comCategoria`, `comStatus`, `comFaixaDeValor` e `build`, verificando o resultado final | Retorna objeto com todos os filtros encadeados corretamente |

# Plano de Testes para Schema (Sprint V)

| Funcionalidade | Comportamento Esperado | Verificações | Critérios de Aceite |
|----------------|-----------------------|--------------|---------------------|
| Validação de dados válidos (cadastro) | Valida corretamente um objeto com todos os campos obrigatórios preenchidos | Fornecer um objeto com `equiNome`, `equiDescricao`, `equiValorDiaria`, `equiQuantidadeDisponivel`, `equiCategoria` e `equiFoto` válidos e verificar se `equipamentoSchema.parse` retorna os dados | Dados são validados; retorna objeto com todos os campos fornecidos |
| Validação de `equiNome` ausente | Lança erro se `equiNome` não for informado | Fornecer objeto sem `equiNome` e verificar se `equipamentoSchema.parse` retorna erro | Lança erro com mensagem "Nome obrigatório" |
| Validação de `equiNome` curto | Lança erro se `equiNome` tiver menos de 2 caracteres | Fornecer objeto com `equiNome` de 1 caractere e verificar se `equipamentoSchema.parse` retorna erro | Lança erro com mensagem "Nome deve ter pelo menos 2 caracteres" |
| Validação de `equiDescricao` ausente | Lança erro se `equiDescricao` não for informada | Fornecer objeto sem `equiDescricao` e verificar se `equipamentoSchema.parse` retorna erro | Lança erro com mensagem "Descrição obrigatória" |
| Validação de `equiValorDiaria` não numérico | Lança erro se `equiValorDiaria` não for número | Fornecer objeto com `equiValorDiaria` como string não numérica e verificar se `equipamentoSchema.parse` retorna erro | Lança erro com mensagem "Valor da diária deve ser um número" |
| Validação de `equiQuantidadeDisponivel` não numérico | Lança erro se `equiQuantidadeDisponivel` não for número | Fornecer objeto com `equiQuantidadeDisponivel` como string não numérica e verificar se `equipamentoSchema.parse` retorna erro | Lança erro com mensagem "Quantidade deve ser um número" |
| Validação de `equiCategoria` inválida | Lança erro se `equiCategoria` não for válida | Fornecer objeto com `equiCategoria` fora das opções permitidas e verificar se `equipamentoSchema.parse` retorna erro | Lança erro com mensagem "Categoria inválida" |
| Validação de `equiFoto` vazio | Lança erro se `equiFoto` for array vazio | Fornecer objeto com `equiFoto` como `[]` e verificar se `equipamentoSchema.parse` retorna erro | Lança erro com mensagem "Pelo menos uma foto é obrigatória" |
| Validação de `equiFoto` com URL inválida | Lança erro se `equiFoto` conter URLs inválidas | Fornecer objeto com `equiFoto` contendo string não URL e verificar se `equipamentoSchema.parse` retorna erro | Lança erro com mensagem "Cada item deve ser uma URL válida" |
| Validação de atualização parcial | Valida corretamente um objeto com campos opcionais permitidos | Fornecer objeto com apenas `equiValorDiaria` e verificar se `equipamentoUpdateSchema.parse` retorna os dados | Dados são validados; retorna objeto com os campos fornecidos |
| Validação de objeto vazio (atualização) | Aceita objeto vazio sem erros | Fornecer objeto vazio (`{}`) e verificar se `equipamentoUpdateSchema.parse` retorna vazio | Retorna objeto vazio (`{}`) sem erros |
| Validação de `equiValorDiaria` negativa (atualização) | Lança erro se `equiValorDiaria` for negativa | Fornecer objeto com `equiValorDiaria` menor que 0 e verificar se `equipamentoUpdateSchema.parse` retorna erro | Lança erro com mensagem "Valor da diária deve ser maior que 0" |
| Validação de `equiQuantidadeDisponivel` negativa (atualização) | Lança erro se `equiQuantidadeDisponivel` for negativa | Fornecer objeto com `equiQuantidadeDisponivel` menor que 0 e verificar se `equipamentoUpdateSchema.parse` retorna erro | Lança erro com mensagem "Quantidade disponível deve ser maior ou igual a 0" |
| Validação de `equiStatus` não booleano (atualização) | Lança erro se `equiStatus` não for booleano | Fornecer objeto com `equiStatus` como string e verificar se `equipamentoUpdateSchema.parse` retorna erro | Lança erro com mensagem "Status deve ser um valor booleano" |
| Validação de campo não permitido (atualização) | Lança erro se incluir campos não permitidos | Fornecer objeto com campo não permitido e verificar se `equipamentoUpdateSchema.parse` retorna erro | Lança erro com mensagem "Campos não permitidos no objeto" |

# Plano de Testes para Schemas Query (Sprint V)

| Funcionalidade | Comportamento Esperado | Verificações | Critérios de Aceite |
|----------------|-----------------------|--------------|---------------------|
| Validação de ID válido | Valida corretamente um ID no formato ObjectId | Fornecer um ID válido e verificar se `EquipamentoIdSchema.parse` retorna o ID | Retorna o ID fornecido sem erros |
| Validação de ID inválido | Lança erro para ID com formato inválido | Fornecer um ID com tamanho incorreto ou caracteres inválidos e verificar se `EquipamentoIdSchema.parse` retorna erro | Lança erro com mensagem "ID inválido" |
| Validação de ID ausente | Lança erro para ID vazio | Fornecer uma string vazia como ID e verificar se `EquipamentoIdSchema.parse` retorna erro | Lança erro com mensagem "ID inválido" |
| Validação de categoria | Valida corretamente uma categoria válida | Fornecer um objeto com `categoria` válida e verificar se `EquipamentoQuerySchema.partial().parse` retorna o objeto | Retorna objeto com `categoria` fornecida |
| Ausência de categoria | Aceita objeto sem categoria | Fornecer um objeto vazio e verificar se `EquipamentoQuerySchema.partial().parse` retorna sem `categoria` | Retorna objeto com `categoria` undefined |
| Validação de status válido | Valida corretamente um status como string "true" ou "false" | Fornecer um objeto com `status` como "true" e verificar se `EquipamentoQuerySchema.partial().parse` retorna `status: true` | Retorna objeto com `status` convertido para booleano |
| Ausência de status | Aceita objeto sem status | Fornecer um objeto vazio e verificar se `EquipamentoQuerySchema.partial().parse` retorna sem `status` | Retorna objeto com `status` undefined |
| Validação de status inválido | Lança erro para status inválido | Fornecer um objeto com `status` inválido e verificar se `EquipamentoQuerySchema.partial().parse` retorna erro | Lança erro |
| Validação de minValor válido | Valida corretamente um valor mínimo numérico | Fornecer um objeto com `minValor` como string numérica  e verificar se `EquipamentoQuerySchema.partial().parse` retorna `minValor: 10` | Retorna objeto com `minValor` convertido para número |
| Validação de minValor negativo | Lança erro para valor mínimo negativo | Fornecer um objeto com `minValor` como e verificar se `EquipamentoQuerySchema.partial().parse` retorna erro | Lança erro com mensagem "minValor deve ser um número inteiro maior que 0" |
| Validação de minValor inválido | Lança erro para valor mínimo não numérico | Fornecer um objeto com `minValor` como "abc" e verificar se `EquipamentoQuerySchema.partial().parse` retorna erro | Lança erro com mensagem "minValor deve ser um número inteiro maior que 0" |
| Ausência de minValor | Aceita objeto sem minValor | Fornecer um objeto vazio e verificar se `EquipamentoQuerySchema.partial().parse` retorna sem `minValor` | Retorna objeto com `minValor` undefined |
| Validação de maxValor válido | Valida corretamente um valor máximo válido | Fornecer um objeto com `maxValor` como string numérica  e verificar se `EquipamentoQuerySchema.partial().parse` retorna `maxValor: 100` | Retorna objeto com `maxValor` convertido para número |
| Validação de maxValor negativo | Lança erro para valor máximo negativo | Fornecer um objeto com `maxValor` como "-1" e verificar se `EquipamentoQuerySchema.partial().parse` retorna erro | Lança erro com mensagem "maxValor deve ser um número maior ou igual a 0" |
| Validação de maxValor inválido | Lança erro para valor máximo não numérico | Fornecer um objeto com `maxValor` como "abc" e verificar se `EquipamentoQuerySchema.partial().parse` retorna erro | Lança erro com mensagem "maxValor deve ser um número maior ou igual a 0" |
| Ausência de maxValor | Aceita objeto sem maxValor | Fornecer um objeto vazio e verificar se `EquipamentoQuerySchema.partial().parse` retorna sem `maxValor` | Retorna objeto com `maxValor` undefined |
| Validação de página válida | Valida corretamente uma página numérica | Fornecer um objeto com `page` como string numérica e verificar se `EquipamentoQuerySchema.partial().parse` retorna `page: 2` | Retorna objeto com `page` convertido para número |
| Validação de página inválida | Lança erro para página inválida (zero ou negativa) | Fornecer um objeto com `page` como "0" e verificar se `EquipamentoQuerySchema.partial().parse` retorna erro | Lança erro com mensagem "Page deve ser um número inteiro maior que 0" |
| Validação de página não numérica | Lança erro para página não numérica | Fornecer um objeto com `page` como "abc" e verificar se `EquipamentoQuerySchema.partial().parse` retorna erro | Lança erro com mensagem "Page deve ser um número inteiro maior que 0" |
| Ausência de página | Aceita objeto sem página | Fornecer um objeto vazio e verificar se `EquipamentoQuerySchema.partial().parse` retorna sem `page` | Retorna objeto com `page` undefined |
| Validação de limite válido | Valida corretamente um limite entre 1 e 100 | Fornecer um objeto com `limit` como string numérica e verificar se `EquipamentoQuerySchema.partial().parse` retorna `limit: 50` | Retorna objeto com `limit` convertido para número |
| Validação de limite fora do intervalo | Lança erro para limite maior que 100 ou menor que 1 | Fornecer um objeto com `limit` como "101" ou "0" e verificar se `EquipamentoQuerySchema.partial().parse` retorna erro | Lança erro com mensagem "Limit deve ser um número inteiro entre 1 e 100" |
| Validação de limite não numérico | Lança erro para limite não numérico | Fornecer um objeto com `limit` como "abc" e verificar se `EquipamentoQuerySchema.partial().parse` retorna erro | Lança erro com mensagem "Limit deve ser um número inteiro entre 1 e 100" |
| Ausência de limite | Aceita objeto sem limite | Fornecer um objeto vazio e verificar se `EquipamentoQuerySchema.partial().parse` retorna sem `limit` | Retorna objeto com `limit` undefined |